# SDD OpenSpec Config — next-proxy-web
# Auto-generated by sdd-init on 2026-06-11

project: next-proxy-web
description: >
  Marketing and documentation landing site for the nextjs-proxy library.
  Static site deployed to Vercel via push-to-main auto-deploy.
  This is one of two repos under the next-proxy monorepo umbrella
  (sibling: next-proxy-package — the library).

stack:
  framework: Next.js 16.2.6 (App Router, RSC)
  language: TypeScript 5.7.3
  styling: Tailwind CSS v4 (PostCSS, @tailwindcss/postcss)
  ui_library: shadcn/ui (base-nova style), @base-ui/react
  icons: lucide-react
  package_manager: pnpm 10.30.1
  monorepo: pnpm workspace (single project, no workspaces configured)

conventions:
  path_aliases:
    "@/*": "./*"
  component_pattern: shadcn-style (PascalCase, @/components/ui)
  utility_pattern: cn() from @/lib/utils (clsx + tailwind-merge)
  css_variables: true (dark mode default)
  imports: no semicolons, single quotes (observed)

strict_tdd: false

testing:
  enabled: false
  reason: >
    No test runner detected. This is a static marketing/landing site.
    No test files, test dependencies, or test configuration found.
  available:
    unit: false
    integration: false
    e2e: false
  coverage: false

quality:
  linter: ESLint (script configured but no eslint dependency in package.json — may not run)
  type_checker: TypeScript strict mode (tsc)
  formatter: None detected

phases:
  propose: openspec/changes/{change-id}/proposal.md
  spec: openspec/changes/{change-id}/spec.md
  design: openspec/changes/{change-id}/design.md
  tasks: openspec/changes/{change-id}/tasks.md
  apply: openspec/changes/{change-id}/apply.md
  verify: openspec/changes/{change-id}/verify.md
  archive: openspec/changes/{change-id}/archive.md -> openspec/changes/archive/

risks:
  - ESLint script configured but eslint package missing from dependencies
  - No formatter — encourages inconsistent code style over time
  - No test safety net — visual regressions go undetected without manual review
  - ignoreBuildErrors: true in next.config — type errors may accumulate silently
